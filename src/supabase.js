// Supabase is intentionally disabled until the backend is set up. Exporting
// null keeps the app entirely on its original login + data path.
export const supabase = null
export const supabaseReady = false
